شات مفتوح المصدر من صنع Qn_RobloxMan بس حط الرابط تبع السكربت او سكربت نفسه في اي مشغل سكربتات تبعه روبلوكس 
قناني : https://youtube.com/@qn_robloxman?si=hXSAzEOZ6Q06auTb





-- roblox delta chat by QnRoblox_man -- V12: Secret Global/Server/Private Edition -- No Downloads / No Replit / Works on Delta local Players = game:GetService("Players") local LocalPlayer = Players.LocalPlayer local HttpService = game:GetService("HttpService") local UserInputService = game:GetService("UserInputService") local ChatService = game:GetService("Chat") local JobId = game.JobId -- [ إعدادات القنوات ] local CurrentMode = "Global" -- الافتراضي local RoomKey = "" local BaseUrl = "https://ntfy.sh/qn_delta_bridge_" local ScreenGui = Instance.new("ScreenGui") ScreenGui.Name = "DeltaSecretChat_V12" ScreenGui.Parent = game.CoreGui ScreenGui.ResetOnSpawn = false ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling local isMobile = UserInputService.TouchEnabled local uiWidthScale = isMobile and 0.5 or 0 local uiWidthOffset = isMobile and 0 or 400 -- [ الأيقونة العائمة ] local ToggleButton = Instance.new("ImageButton") ToggleButton.Parent = ScreenGui ToggleButton.Size = UDim2.new(0, 50, 0, 50) ToggleButton.Position = UDim2.new(0.05, 0, 0.2, 0) ToggleButton.BackgroundColor3 = Color3.fromRGB(30, 30, 30) ToggleButton.BackgroundTransparency = 0.4 ToggleButton.Image = "rbxassetid://12696048454" ToggleButton.Draggable = true ToggleButton.Active = true Instance.new("UICorner", ToggleButton).CornerRadius = UDim.new(0.3, 0) -- [ الإطار الرئيسي ] local MainFrame = Instance.new("Frame") MainFrame.Parent = ScreenGui MainFrame.BackgroundColor3 = Color3.fromRGB(30, 32, 35) MainFrame.BackgroundTransparency = 0.2 MainFrame.Position = UDim2.new(0.2, 0, 0.5, 0) MainFrame.Size = UDim2.new(uiWidthScale, uiWidthOffset, 0, 250) MainFrame.Visible = false MainFrame.Active = true MainFrame.Draggable = true Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 12) -- [ شريط العنوان ] local TitleLabel = Instance.new("TextLabel", MainFrame) TitleLabel.Size = UDim2.new(1, -20, 0, 25) TitleLabel.Position = UDim2.new(0, 10, 0, 5) TitleLabel.Text = "DELTA SECRET CHAT" TitleLabel.Font = Enum.Font.GothamBold TitleLabel.TextColor3 = Color3.fromRGB(200, 200, 200) TitleLabel.TextSize = 12 TitleLabel.BackgroundTransparency = 1 TitleLabel.TextXAlignment = Enum.TextXAlignment.Left -- [ أزرار الرومات - تصميم مدمج ] local TabFrame = Instance.new("Frame", MainFrame) TabFrame.Size = UDim2.new(1, -20, 0, 25) TabFrame.Position = UDim2.new(0, 10, 0, 30) TabFrame.BackgroundTransparency = 1 local function CreateTab(name, pos, color) local btn = Instance.new("TextButton", TabFrame) btn.Size = UDim2.new(0.32, 0, 1, 0) btn.Position = UDim2.new(pos, 0, 0, 0) btn.Text = name btn.Font = Enum.Font.GothamBold btn.TextSize = 10 btn.BackgroundColor3 = color btn.TextColor3 = Color3.fromRGB(255, 255, 255) Instance.new("UICorner", btn) return btn end local GBtn = CreateTab("GLOBAL", 0, Color3.fromRGB(50, 80, 200)) local SBtn = CreateTab("SERVER", 0.34, Color3.fromRGB(50, 150, 80)) local RBtn = CreateTab("PRIVATE", 0.68, Color3.fromRGB(150, 50, 100)) -- [ منطقة الرسائل ] local ChatLogs = Instance.new("ScrollingFrame", MainFrame) ChatLogs.Size = UDim2.new(1, -10, 1, -110) ChatLogs.Position = UDim2.new(0, 5, 0, 60) ChatLogs.BackgroundTransparency = 1 ChatLogs.ScrollBarThickness = 2 local UIList = Instance.new("UIListLayout", ChatLogs) UIList.SortOrder = Enum.SortOrder.LayoutOrder UIList.Padding = UDim.new(0, 2) -- [ حاوية الكتابة ] local InputContainer = Instance.new("Frame", MainFrame) InputContainer.Size = UDim2.new(1, -14, 0, 36) InputContainer.Position = UDim2.new(0, 7, 1, -42) InputContainer.BackgroundColor3 = Color3.fromRGB(0, 0, 0) InputContainer.BackgroundTransparency = 0.6 Instance.new("UICorner", InputContainer).CornerRadius = UDim.new(0, 18) local TextBox = Instance.new("TextBox", InputContainer) TextBox.Size = UDim2.new(1, -45, 1, 0) TextBox.Position = UDim2.new(0, 10, 0, 0) TextBox.PlaceholderText = "https://www.youtube.com/@Qn_RobloxMan" TextBox.PlaceholderColor3 = Color3.fromRGB(150, 150, 150) TextBox.TextColor3 = Color3.fromRGB(255, 255, 255) TextBox.TextSize = 13 TextBox.Font = Enum.Font.GothamMedium TextBox.BackgroundTransparency = 1 TextBox.TextXAlignment = Enum.TextXAlignment.Left local SendBtn = Instance.new("ImageButton", InputContainer) SendBtn.Size = UDim2.new(0, 24, 0, 24) SendBtn.Position = UDim2.new(1, -30, 0, 6) SendBtn.BackgroundTransparency = 1 SendBtn.Image = "rbxassetid://7733964719" SendBtn.ImageColor3 = Color3.fromRGB(200, 200, 200) -- [ الوظائف البرمجية ] local function GetUrl() if CurrentMode == "Global" then return BaseUrl .. "global" elseif CurrentMode == "Server" then return BaseUrl .. "srv_" .. JobId else return BaseUrl .. "priv_" .. RoomKey end end local function AddMessage(user, text) local msgContainer = Instance.new("Frame", ChatLogs) msgContainer.BackgroundTransparency = 1 msgContainer.AutomaticSize = Enum.AutomaticSize.Y msgContainer.Size = UDim2.new(1, 0, 0, 0) local textLabel = Instance.new("TextLabel", msgContainer) textLabel.Text = string.format("[%s]: %s", user, text) textLabel.TextColor3 = Color3.fromRGB(255, 255, 255) textLabel.Font = Enum.Font.GothamMedium textLabel.TextSize = 14 textLabel.BackgroundTransparency = 1 textLabel.TextWrapped = true textLabel.Size = UDim2.new(1, 0, 0, 0) textLabel.AutomaticSize = Enum.AutomaticSize.Y textLabel.TextXAlignment = Enum.TextXAlignment.Left if user == LocalPlayer.Name then textLabel.RichText = true textLabel.Text = string.format("[%s]: %s", user, text) end task.wait() ChatLogs.CanvasSize = UDim2.new(0, 0, 0, UIList.AbsoluteContentSize.Y) ChatLogs.CanvasPosition = Vector2.new(0, UIList.AbsoluteContentSize.Y) end local function SendChat() local msg = TextBox.Text if msg == "" or string.match(msg, "^%s*$") then return end if CurrentMode == "Private" and RoomKey == "" then AddMessage("System", "Error: Enter a Room Key first!") return end -- إرسال عبر ntfy (الرابط السري) task.spawn(function() local req = (syn and syn.request) or request or http_request req({ Url = GetUrl(), Method = "POST", Body = LocalPlayer.Name .. ": " .. msg }) end) AddMessage(LocalPlayer.Name, msg) -- إظهارها لك فوراً TextBox.Text = "" end -- [ تبديل الأوضاع ] GBtn.MouseButton1Click:Connect(function() CurrentMode = "Global" AddMessage("System", "Mode: GLOBAL CHAT (All Servers)") end) SBtn.MouseButton1Click:Connect(function() CurrentMode = "Server" AddMessage("System", "Mode: SERVER CHAT (This JobId only)") end) RBtn.MouseButton1Click:Connect(function() CurrentMode = "Private" AddMessage("System", "Type the Room Key in Chat to Join!") end) -- [ استقبال الرسائل ] task.spawn(function() local lastTime = os.time() while true do local req = (syn and syn.request) or request or http_request pcall(function() local res = req({Url = GetUrl() .. "/json?poll=1&since=" .. lastTime, Method = "GET"}) if res.Success and res.Body ~= "" then for line in res.Body:gmatch("[^\r\n]+") do local data = HttpService:JSONDecode(line) if data.message then local parts = data.message:split(": ") if parts[1] ~= LocalPlayer.Name then AddMessage(parts[1], parts[2] or "") end end end lastTime = os.time() end end) task.wait(2) end end) SendBtn.MouseButton1Click:Connect(SendChat) TextBox.FocusLost:Connect(function(enter) if enter then if CurrentMode == "Private" and RoomKey == "" then RoomKey = TextBox.Text AddMessage("System", "Joined Room: " .. RoomKey) TextBox.Text = "" else SendChat() end end end) ToggleButton.MouseButton1Click:Connect(function() MainFrame.Visible = not MainFrame.Visible end) AddMessage("System", "Secret Chat Loaded - Design by Qn_RobloxMan")
